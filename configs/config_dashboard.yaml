# 대시보드 설정
dashboard:
  # Streamlit 설정
  streamlit:
    page_title: "AIOps Real-time Monitor"
    page_icon: "🔍"
    layout: "wide"
    
  # 차트 설정
  charts:
    # 실시간 업데이트 간격 (초)
    update_interval: 1.0
    # 표시할 데이터 포인트 수
    max_data_points: 500
    # 차트 높이
    height: 400
    
  # 알림 설정
  alerts:
    # 알림 표시 시간 (초)
    display_duration: 30
    # 최대 알림 수
    max_alerts: 50

